#include<stdio.h>
/**
 * main - A program that prints puts function in a line
 * reurn:0 when the program returns the val zero that shows we succeeded
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
